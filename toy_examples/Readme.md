# Toy examples for input
> [!WARNING]
> Should be used only to check the performance of the App and to refer to the data format of inputs.

---
- `xcms` table output was generated as follows:
```r
library(xcms)

pd <- data.frame(...)
xdata <- readMsExperiment(...) or readMSData(...)

xdata <- findChromPeaks(...)
xdata <- adjustRtime(...)
xdata <- groupChromPeaks(...)
xdata <- fillChromPeaks(...)

fvals <- featureValues(xdata, method = "sum")
fdef <- featureDefinitions(mse)[, c("mzmed", "mzmin", "mzmax", "rtmed", "rtmin", "rtmax")]
fvals <- cbind(Row.names = rownames(fdef), fdef, fvals)

write.csv(fvals, "... .csv", row.names = F)
```
---
- `mzMine` output was generated by the module *`Export molecular network files; Options: no normalization, Intensity - Area, CSV export - Simple`*. File ends with *`_quant.csv`*.
---
- `MS-Dial` output was generated by the module *`Export -> Alignment result -> Selected Options: Raw data (Area); format csv; trim conten
